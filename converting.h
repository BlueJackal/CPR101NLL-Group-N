#ifndef _CONVERTING_H_            //make sure no other converting.h file is defined
#define _CONVERTING_H_                
#include <stdio.h>                //include our libraries for the functions we'll be using
#include <string.h>
#include <stdlib.h>                
void converting(void);            //create the prototype for converting
#endif                            //endif